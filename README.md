# OpenAttend
OpenAttend is an open-source, self-hosted attendance management system. Any organization can spin up their own server (on their own hardware a laptop, a VPS, an on-prem box) and manage attendance for their members through an Android app. There is no central hosted service each org owns and runs its own instance.
